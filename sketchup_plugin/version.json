{
  "version": "0.2.7",
  "rb_url": "https://raw.githubusercontent.com/hohomehk/sketchupdrawing/main/sketchup_plugin/su_gpt_render/su_gpt_render.rb",
  "notes": "v0.2.7 — Fix: silent auto-update did not fire on v0.2.5/0.2.6 because UI.start_timer was called from a background Thread (unreliable in SketchUp Ruby). Now we schedule all timers on the main thread and use a polling timer to check the HTTP-check thread state, then apply on main."
}
