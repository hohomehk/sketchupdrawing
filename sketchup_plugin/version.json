{
  "version": "0.5.8",
  "rb_url": "https://hohomehk-plugin.ngrok.app/su_gpt_render.rb",
  "notes": "v0.5.8 — Live Render preview now shows BOTH input views alongside the AI render. Three-cell grid: Hidden Line (geometry conditioning) · Shaded (material reference) · AI Render output. In single-view mode the geometry cell stays empty so the layout doesn't shift. Click any cell to open the full-size PNG. Tiny corner labels make it obvious which conditioning role each view plays."
}
