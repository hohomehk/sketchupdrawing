{
  "version": "0.4.4",
  "rb_url": "https://github.com/hohomehk/sketchupdrawing/releases/download/v0.4.4/su_gpt_render.rb",
  "notes": "v0.4.4 — Live Stream output now persists across frames. setLiveFrame was wiping live_output every tick (~every 2s), so the streamed text appeared and immediately disappeared. Now each frame's reply lands in its own timestamped paragraph; older replies stay visible. Output area is scrollable (max-height 280px). Start Live still clears the log."
}
